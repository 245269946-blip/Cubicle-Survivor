# Versions

## v1.0-legacy

旧线上版归档，位于 `v1.0-legacy/`。

定位：

- 武器升级是主要成长线。
- 道具/被动提供主要 Build 变化。
- 属性成长作为长期数值支撑。

该目录只用于归档、对比和必要回滚，不作为当前 v2.0 主线开发目录。

## v2.0 active prototype

当前开发入口仍保留在仓库根目录：

- `index.html`
- `main.js`
- `styles.css`
- `src/`
- `assets/`

等 v2.0 主循环稳定后，再考虑把运行时代码整体迁入 `versions/v2.0-*`。
